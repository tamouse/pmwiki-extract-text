require 'spec_helper'

describe Pmwiki::Extract::Text do
  it 'has a version number' do
    expect(Pmwiki::Extract::Text::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
